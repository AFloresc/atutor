<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.inredec.atutor">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentatiolayer.controllers.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentatiolayer.controllers.activities.LoginActivity" />
        <activity android:name=".presentatiolayer.controllers.activities.TestActivity" />
        <activity android:name=".ConceptsActivity" />
        <activity android:name=".presentatiolayer.controllers.activities.AddUserTestActivity" />
        <activity android:name=".presentatiolayer.controllers.activities.MainAppActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentatiolayer.controllers.activities.LoginActivity2"
            android:label="@string/title_activity_login2" />
        <activity android:name=".presentatiolayer.controllers.activities.LessonDetailActivity"></activity>
    </application>

</manifest>